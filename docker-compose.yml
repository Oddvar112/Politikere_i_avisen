services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - MYSQLUSER=${MYSQLUSER}
      - MYSQLPASSWORD=${MYSQLPASSWORD}
    volumes:
      - model-cache:/root/.cache/news-analyzer

volumes:
  model-cache:
